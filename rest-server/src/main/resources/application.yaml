server:
  port: 8080
  host: "0.0.0.0"

db:
  source: "jdbc"
  connection:
    url: "jdbc:postgresql://localhost:5432/DbClient"
    username: "postgres"
    password: "postgres"
  hikari:
    maximum-pool-size: 10
    minimum-idle: 2
